{
  "name": "wattlyzer",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=22.12.0",
    "bun": ">=1.3.14"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "nx run-many -t dev --projects=pwa,website --parallel=2",
    "dev:pwa": "nx dev pwa",
    "dev:website": "nx dev website",
    "build": "nx run-many -t build",
    "lint": "nx run-many -t lint",
    "typecheck": "nx run-many -t typecheck",
    "test": "nx run-many -t test",
    "check": "nx run-many -t lint,typecheck,test,build,verify-seo",
    "affected": "nx affected -t lint,typecheck,test,build,verify-seo"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@nx/devkit": "latest",
    "@nx/eslint-plugin": "latest",
    "@types/bun": "^1.4.2",
    "@types/node": "^25.9.8",
    "eslint": "^10.11.0",
    "eslint-plugin-astro": "latest",
    "nx": "latest",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.70.1"
  },
  "trustedDependencies": [
    "unrs-resolver"
  ]
}
